# BuildTools — 多边形建筑生成工具

## 概述

BuildTools 是一个 Unity Editor 工具，用于在场景中绘制多边形边界，并沿边界随机排列 Prefab 建筑。适用于快速搭建城市场景、园区布局、公交站周边建筑等。

## 文件结构

```
Assets/BuildTools/
├── BuildToolsSettings.cs          # ScriptableObject 数据模型
├── Editor/
│   ├── BuildToolsWindow.cs       # EditorWindow 主窗口 + Scene View 交互
│   ├── BuildToolsPlacement.cs    # 放置算法与实例化逻辑
│   └── ZebraFillShader.shader    # 斑马纹填充 Shader
└── Settings/
    └── BuildToolsSettings.asset  # 自动生成的持久化设置
```

## 快速上手

### 1. 打开工具窗口

Unity 菜单 → **Window > Build Tools**

窗口顶部有一个 **「进入编辑」/「退出编辑」** 按钮：
- 点击进入后，工具开始拦截场景视图点击（Q/W 切换模式）
- 点击退出后，恢复 Unity 正常操作，不会再误触添加顶点

### 2. 两种工作模式

工具通过 Unity 原生工具栏切换，与 Unity 快捷键一致：

| 快捷键 | Unity 工具 | 对应模式 | 功能 |
|--------|-----------|----------|------|
| **Q** | View（手形） | **添加模式** | 点击地面添加顶点，顶点显示为绿色圆点 |
| **W** | Move（移动） | **编辑模式** | 顶点出现 XYZ 坐标轴，可沿轴拖拽移动 |
| E/R/T 等 | 其他工具 | 禁用 | 场景交互暂停 |

### 3. 添加模式（Q）— 绘制多边形

1. 按 **Q** 确认工具栏为手形图标
2. 在 **Scene View** 中**左键点击地面**，每次点击添加一个顶点
3. 连续顶点之间会自动绘制黄色边线
4. 最后一个顶点到鼠标位置会显示**虚线预览**
5. **Shift + 点击边**：在已有线段上插入一个新顶点（边会高亮为绿色）
6. 窗口状态栏显示 `🟢 添加模式（按 Q）`

### 4. 编辑模式（W）— 调整顶点

1. 按 **W**，工具栏会短暂显示移动图标后恢复手形（工具内部重置为 None 以避免冲突）
2. 每个顶点出现 **XYZ 坐标轴**（PositionHandle）
3. **点击并拖拽**坐标轴可沿轴精确移动顶点位置
4. 移动后 Y 坐标会自动投射回地面
5. 窗口状态栏显示 `🟡 编辑模式（按 W）`
6. 编辑模式下**点击地面不会添加新顶点**

### 5. 闭合多边形

按 **Enter** 键或点击窗口中的「闭合多边形」按钮。闭合后会出现**半透明黄色填充**。

### 6. 配置建筑 Prefab

在窗口的 **「建筑 Prefabs」** 区域：

- 点击 **「+ 添加 Prefab 槽位」**
- 拖入建筑 Prefab 到对象字段
- 调整**权重**（0-1），权重越高被选中的概率越大
- 可添加多个槽位，支持混合多种建筑

### 7. 生成建筑

点击 **「🎲 生成建筑」** 按钮，或开启 **「实时预览」** 开关。

开启实时预览后，调节任何参数都会自动重新生成建筑（0.3 秒防抖），无需手动点击生成按钮。

## 快捷键

| 快捷键 | 模式 | 功能 |
|--------|------|------|
| **Q** | 切换 | 进入**添加模式**（View Tool），点击地面添加顶点 |
| **W** | 切换 | 进入**编辑模式**（Move Tool），顶点出现坐标轴 |
| 鼠标左键点击地面 | 添加 | 在末尾添加顶点 |
| **Shift + 点击边** | 添加 | 在已有线段上插入新顶点 |
| **Ctrl + 点击顶点** | 添加 | 删除该顶点（变红提示） |
| 拖拽坐标轴 | 编辑 | 沿 XYZ 轴精确移动顶点 |
| **Enter** | 通用 | 闭合 / 打开多边形 |
| **Esc** | 通用 | 撤销最后一个顶点 |
| **Delete** / **Backspace** | 通用 | 删除最后一个顶点 |
| **C** | 通用 | 切换闭合状态 |
| **F** | 通用 | 框选多边形（聚焦相机） |

## 窗口 UI 说明

### 多边形区域

- 显示所有顶点的世界坐标，可直接编辑数值
- 每个顶点可单独删除（点击 × 按钮）
- 「闭合多边形」/「打开多边形」— 切换闭合状态
- 「清空顶点」— 删除所有顶点
- 「撤销上一个」— 移除最后一个顶点

### 建筑 Prefabs 区域

- 每个槽位包含：Prefab 对象字段、权重滑块、启用开关、删除按钮
- 最多可添加任意数量的槽位

### 放置参数

| 参数 | 说明 | 默认值 |
|------|------|--------|
| **排列模式** | 随机 / 等距 / 顺序 | 随机 |
| **间距** | 等距/顺序模式下的**边缘到边缘**空隙（0=自动按数量等分周长，中心到中心） | 0 |
| **建筑数量** | 沿边缘放置的建筑总数（等距间距为0时生效） | 10 |
| **随机种子** | 相同种子产生相同结果（仅随机模式） | 0 |
| **边缘偏移** | 正值=向多边形内部偏移，负值=向外部偏移 | 0 |
| **最小间距** | 建筑之间的最小距离（0=不检测） | 0 |
| **朝向模式** | 见下方说明 | 面向中心 |
| **随机旋转** | 在基朝向基础上添加随机旋转偏移 | 关闭 |
| **旋转范围** | 随机旋转的角度范围（±°） | 15° |
| **覆盖图层** | 为生成的建筑设置统一图层 | 关闭 |
| **分组建筑** | 将所有生成的建筑放在同一个父对象下 | 开启 |
| **分组名称** | 父 GameObject 的名称 | _Buildings |

### 朝向模式

| 模式 | 说明 |
|------|------|
| **FacePolygonCenter** | 建筑前方朝向多边形内部中心 |
| **AlongEdge** | 建筑前方沿边缘切线方向 |
| **WorldDirection** | 固定世界方向（可自定义） |
| **Random** | 完全随机 Y 轴旋转 |

### 可视化设置

- **轮廓颜色** — 多边形边线颜色（默认黄色）
- **填充颜色** — 斑马纹主色，第二种颜色自动由主色降低透明度生成
- **手柄颜色** — 顶点球形手柄颜色（默认绿色）
- **手柄大小** — 顶点手柄的屏幕尺寸
- **预览线颜色** — 未闭合时从最后顶点到鼠标的虚线颜色
- **射线检测图层** — 点击添加顶点时射线检测的图层

## 放置算法说明

### 随机模式 (Random)
1. 计算多边形每条边在 XZ 平面上的长度
2. 按边长加权随机选择边（长边被选中的概率更高）
3. 在选中的边上随机采样位置
4. 应用边缘偏移 + 计算朝向 + 选择 Prefab
5. 检测最小间距约束（不满足则重试，最多 10 次）

### 等距模式 (Equidistant)
1. 计算总周长，按间距或数量确定步长
2. 随机起始偏移，沿周长均匀分布
3. 每个步长位置定位到对应边，计算坐标和朝向

### 顺序模式 (Sequential)
1. 从多边形第一个顶点（索引 0）出发
2. 按固定间距沿边缘方向依次放置
3. 跨边自动衔接，直到绕完整个周长
4. 间距为 0 时自动按建筑数量等分周长

## Undo 支持

所有操作均支持 **Ctrl+Z** 撤销：

- 添加 / 移动 / 删除顶点
- 切换闭合状态
- 修改任何参数
- 生成建筑（逐个注册）
- 清除建筑（逐个销毁）

## 技术细节

- **Unity 版本**: 2022.3+
- **渲染管线**: Built-in Render Pipeline
- **多边形数学**: 所有计算在 XZ 平面上进行，Y 坐标通过射线检测获取
- **填充渲染**: 使用自定义 ZebraFill Shader 通过 Mesh + `Graphics.DrawMeshNow` 绘制双色斑马纹，条纹沿世界空间 XZ 对角线方向
- **设置持久化**: ScriptableObject 存储在 `Assets/BuildTools/Settings/`
- **场景引用**: 通过隐藏的 `BuildToolsSceneSettings` GameObject 跟踪生成的建筑

## 注意事项

- 添加顶点前确保场景中有可射线检测的地面/碰撞体
- 如果没有命中碰撞体，顶点会自动投影到 **Y=0** 平面
- 多边形必须闭合（≥3 个顶点）才能生成建筑
- 至少需要一个启用的、包含 Prefab 的槽位才能生成
- 凹多边形填充显示可能有轻微视觉瑕疵，但不影响放置精度
